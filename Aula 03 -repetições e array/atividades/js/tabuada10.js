const numero = 1;

console.log(`${numero}:`);

for (let contador = 1; contador <= 10; contador++) {
  const resultado = numero * contador;
  console.log(`${numero} x ${contador} = ${resultado}`);
}

const numero2 = 2;

console.log(`${numero2}:`);

for (let contador = 1; contador <= 10; contador++) {
  const resultado = numero2 * contador;
  console.log(`${numero2} x ${contador} = ${resultado}`);
}

const numero3 = 3;

console.log(`${numero3}:`);

for (let contador = 1; contador <= 10; contador++) {
  const resultado = numero3 * contador;
  console.log(`${numero2} x ${contador} = ${resultado}`);
}

const numero4 = 4;

console.log(`${numero4}:`);

for (let contador = 1; contador <= 10; contador++) {
  const resultado = numero4 * contador;
  console.log(`${numero2} x ${contador} = ${resultado}`);
}

const numero5 = 5;

console.log(`${numero5}:`);

for (let contador = 1; contador <= 10; contador++) {
  const resultado = numero5 * contador;
  console.log(`${numero2} x ${contador} = ${resultado}`);
}
const numero6 = 6;

console.log(`${numero6}:`);

for (let contador = 1; contador <= 10; contador++) {
  const resultado = numero6 * contador;
  console.log(`${numero2} x ${contador} = ${resultado}`);
}
const numero7 = 7;

console.log(`${numero7}:`);

for (let contador = 1; contador <= 10; contador++) {
  const resultado = numero7 * contador;
  console.log(`${numero2} x ${contador} = ${resultado}`);
}

const numero8 = 8;

console.log(`${numero8}:`);

for (let contador = 1; contador <= 10; contador++) {
  const resultado = numero8 * contador;
  console.log(`${numero2} x ${contador} = ${resultado}`);
}

const numero9 = 9;

console.log(`${numero9}:`);

for (let contador = 1; contador <= 10; contador++) {
  const resultado = numero9 * contador;
  console.log(`${numero2} x ${contador} = ${resultado}`);
}

const numero10 = 10;

console.log(`${numero10}:`);

for (let contador = 1; contador <= 10; contador++) {
  const resultado = numero10 * contador;
  console.log(`${numero2} x ${contador} = ${resultado}`);
}


// Correção

// for (var contTabuada = 1; contTabuada <=10; contTabuada++){
//   console.log("-----------")
// for (var cont = 1; cont <=10; cont++){
//   console.log(`${cont} x ${contTabuada} = ${cont * contTabuada}`);
// }
// console.log("-----------")
// }